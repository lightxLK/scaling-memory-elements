import type { ComponentMeta } from '@/types'

const meta: ComponentMeta = {
  slug: 'specular-button',
  name: 'Specular Button',
  description: 'Glass button with a WebGL specular highlight that sweeps along the edge, steering toward the cursor.',
  category: 'Hover',
  tags: ['button', 'webgl', 'shader', 'glass', 'specular'],
  family: 'react-bits',
  deps: ['ogl'],
  runnable: true,
  background: 'dark',
}

export default meta
